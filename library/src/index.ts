// Keep the package root as the backwards-compatible complete API. Purpose-specific
// consumers can use `betterdocx/core` or `betterdocx/patcher` directly.
export * from "./core";
export * from "./patcher";
